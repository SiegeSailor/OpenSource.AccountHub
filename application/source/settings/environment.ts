const {
  DATABASE_HOST,
  DATABASE_USER,
  DATABASE_PASSWORD,
  DATABASE_CONNECTION_LIMIT = 10,
  SECRET = "",
  HTTPS,
  PORT = 3000,
} = process.env;

export default {
  DATABASE_HOST,
  DATABASE_USER,
  DATABASE_PASSWORD,
  DATABASE_CONNECTION_LIMIT: Number(DATABASE_CONNECTION_LIMIT),
  SECRET,
  HTTPS: Boolean(HTTPS),
  PORT: Number(PORT),
};
